# Transformation Pulse Global — Hybrid Data Strategy & Self-Healing

# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Job Sourcing (Apify LinkedIn Jobs Scraper)
APIFY_TOKEN=your-apify-token
APIFY_LINKEDIN_JOBS_ACTOR=bebity/linkedin-jobs-scraper

# People Discovery (Apollo.io)
APOLLO_API_KEY=your-apollo-api-key

# Self-Healing (LLM selector recovery)
OPENAI_API_KEY=your-openai-api-key

# Proxies (Zyte / Bright Data) — configure in Apify or scraper runtime
# ZYTE_API_KEY=...
# BRIGHT_DATA_...=...

# Stealth Governor / Anti-Ban (Phase 2 scrapers)
# ENCRYPTION_KEY=...  # 32-byte hex for cookie encryption (openssl rand -hex 32)
# FINGERPRINT_API_KEY=...  # Server API (Auth-API-Key). If Bot Score > 10%, rotate proxy + UA
# FINGERPRINT_LAST_REQUEST_ID=...  # From frontend agent (optional). PROXY_*_ALT for rotation
# CAMOUFOX_EXECUTABLE_PATH=...  # For KSA/SG; USE_CAMOUFOX=1 or auto for Riyadh/Singapore
# Playwright-Stealth + Camoufox for KSA/SG bank portals (TLS/JA3 bypass)
# my_history.json: Local-First only; never send raw contact list to public LLM

# Cron (production)
# CRON_SECRET=...  # Bearer token for GET /api/cron/refresh-hubs

# Observability
# PHOENIX_COLLECTOR_URL=...  # OTLP endpoint for Arize Phoenix

# PhantomBuster (LinkedIn Partner discovery)
# PHANTOMBUSTER_API_KEY=...
# PHANTOMBUSTER_LINKEDIN_SEARCH_AGENT_ID=...

# Proxy (per hub or global)
# ZYTE_PROXY=... ZYTE_API_KEY=...
# PROXY_RIYADH=... PROXY_SG=...
# USE_CAMOUFOX=1  # Use Camoufox for Riyadh/Singapore
